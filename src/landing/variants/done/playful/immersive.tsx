'use client';
import type { DoneSlotProps } from '../../../slots';
import { ContactInfoWithVideoDone } from '../contact-info-with-video';

export function PlayfulImmersiveDone(props: DoneSlotProps) {
  return <ContactInfoWithVideoDone {...props} />;
}
